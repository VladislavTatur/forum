import { Users } from '@shared/components/users/Users.tsx';

import { Card } from '../shared/components/Card.tsx';

export const UsersPage = () => {
  return (
    <Card>
      <Users />
    </Card>
  );
};
