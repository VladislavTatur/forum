import { MainUser } from '@features/users/components/MainUser.tsx';
import { Card } from '@shared/components/Card.tsx';

const ProfilePage = () => {
  return (
    <Card>
      <MainUser />
    </Card>
  );
};

export default ProfilePage;
